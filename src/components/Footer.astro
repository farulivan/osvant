---
// Footer (01-design-system.md §5.6). Lives OUTSIDE [data-taxi] — persistent
// across navigations, same as Nav.astro.
import { withBase } from "../lib/url";

const sitemap = [
  { href: "collection", label: "collection" },
  { href: "the-house", label: "the house" },
  { href: "journal", label: "journal" },
  { href: "contact", label: "contact" },
];

const legal = [
  { href: "legal/privacy", label: "privacy" },
  { href: "legal/terms", label: "terms" },
];

const socials = [
  { href: "https://instagram.com", label: "instagram" },
  { href: "https://tiktok.com", label: "tiktok" },
  { href: "https://youtube.com", label: "youtube" },
  { href: "https://twitch.tv", label: "twitch" },
];
---

<footer class="footer" data-nav-theme="dark">
  <div class="footer__marquee" aria-hidden="true" data-anim="marquee">
    <span>provocation, bottled. &nbsp;</span>
    <span>provocation, bottled. &nbsp;</span>
  </div>

  <div class="footer__newsletter">
    <p class="footer__newsletter-label">join the current</p>
    <form class="footer__newsletter-form" data-newsletter-form>
      <label class="visually-hidden" for="newsletter-email">Email</label>
      <input
        id="newsletter-email"
        name="email"
        type="email"
        placeholder="your email"
        required
      />
      <button class="btn-primary" type="submit">
        <span data-btn-label>join</span>
      </button>
      <p class="newsletter-msg" data-newsletter-msg role="status" hidden></p>
    </form>
    <p class="footer__newsletter-success" data-newsletter-success hidden>
      you&rsquo;re in the current.
      <span class="footer__newsletter-demo">demo — no list connected.</span>
    </p>
  </div>

  <div class="footer__rows">
    <ul class="footer__list">
      {
        sitemap.map((item) => (
          <li>
            <a href={withBase(item.href)}>{item.label}</a>
          </li>
        ))
      }
    </ul>

    <ul class="footer__list">
      {
        socials.map((item) => (
          <li>
            <a href={item.href} target="_blank" rel="noreferrer">
              {item.label}
            </a>
          </li>
        ))
      }
    </ul>

    <ul class="footer__list">
      {
        legal.map((item) => (
          <li>
            <a href={withBase(item.href)}>{item.label}</a>
          </li>
        ))
      }
    </ul>
  </div>

  <p class="footer__wordmark" aria-hidden="true">osvant</p>
</footer>

<style>
  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip-path: inset(50%);
    white-space: nowrap;
  }

  .footer {
    background: var(--color--black);
    color: var(--color--white);
    padding: 6rem var(--gutter) 0;
    overflow: hidden;
  }

  /* Ghost type (marquee + wordmark): 01 §5.6 specifies no color — gap
     flagged. ink-2 on black fails WCAG 1.4.3 (1.63:1); this lilac-3 mix
     keeps the ghosted look at ~4:1 (≥3:1 large-text floor). */
  .footer__marquee {
    display: flex;
    white-space: nowrap;
    font-size: var(--text--impact);
    font-weight: 900;
    color: color-mix(in oklab, var(--color--lilac-3) 60%, var(--color--black));
  }

  .footer__newsletter {
    margin-top: 4rem;
  }

  .footer__newsletter-label {
    font-size: var(--text--h4);
    font-family: "Instrument Serif", serif;
    font-style: italic;
  }

  .footer__newsletter-form {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    max-width: 28rem;
    overflow: hidden;
  }

  /* 01 §5.5 — validation copy at eyebrow size, amber, below the field */
  .newsletter-msg {
    flex-basis: 100%;
    margin: 0.25rem 0 0;
    font-size: var(--text--eyebrow);
    letter-spacing: 0.12em;
  }

  .newsletter-msg--error {
    color: var(--color--amber);
  }

  .footer__newsletter-success {
    margin: 0.5rem 0 0;
    font-family: "Instrument Serif", serif;
    font-style: italic;
    font-size: var(--text--h4);
  }

  .footer__newsletter-demo {
    display: block;
    margin-top: 0.25rem;
    font-family: inherit;
    font-size: var(--text--eyebrow);
    letter-spacing: 0.12em;
    font-style: normal;
    color: var(--color--lilac-3);
  }

  .footer__newsletter-form input {
    flex: 1;
    background: transparent;
    border: 0;
    border-bottom: 1px solid var(--color--ink-3);
    border-radius: 0;
    color: var(--color--white);
    padding: 0.5rem 0;
    font-size: var(--text--body);
  }

  .footer__newsletter-form input:focus-visible {
    border-bottom-color: var(--color--uv);
  }

  .footer__rows {
    display: flex;
    flex-wrap: wrap;
    gap: 3rem;
    margin-top: 4rem;
    font-size: var(--text--h6);
  }

  .footer__list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .footer__list a {
    color: var(--color--lilac-3);
    text-decoration: none;
  }

  .footer__list a:hover {
    color: var(--color--white);
  }

  .footer__wordmark {
    margin: 4rem 0 0;
    font-size: clamp(6rem, 20vw, 16rem);
    font-weight: 900;
    line-height: 0.8;
    color: color-mix(in oklab, var(--color--lilac-3) 60%, var(--color--black));
    transform: translateY(20%);
  }
</style>
