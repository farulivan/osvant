---
# Pipeline-fixture entry (ADR-010): exercises the collection → index →
# article render path end-to-end. Replaced by the real launch articles
# when the w4 copy batch lands (RFC-001 C1).
title: "batch 002 — fall [draft]"
date: 2026-09-01
leadImage:
  src: "" # photography pending (ADR-008) — template renders the wash
  alt: "batch 002 campaign still"
scent: fever
---

The `next drop` chip on the home hero deep-links here (RFC-001 B6).

> a pull-quote renders in Instrument Serif italic — the second register.

Body copy ships with the w4 batch. This entry exists to prove the
pipeline: zod schema, build-time render, card on the index, cross-sell
band at the foot.
