# --- AI tooling (local only; process documented in AGENTS.md / README) ---
.agents/
.claude/
.windsurf/
skills-lock.json

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# --- Node / build (for the upcoming scaffold) ---
node_modules/
dist/
.astro/
*.log
.env
.env.*
!.env.example

# --- Parity reference recordings + capture tooling (local-only dev reference, R11) ---
docs/parity-recordings/*.webm
scripts/capture-parity.mjs

# --- Font masters (01 §3.1) ---
# Licensed .otf originals. Only the subset woff2 in src/assets/fonts/ ships,
# and only that is committed. Regenerate with `pnpm fonts`; inspect with
# `pnpm fonts:inspect`.
src/assets/fonts/masters/*
!src/assets/fonts/masters/README.md

# --- Test & perf artifacts ---
coverage/
playwright-report/
test-results/
.lighthouseci/
